import re
from collections import Counter


def load_text(filepath):
    """Read the entire file and return its contents as one string."""


def tokenize_words(text):
    """Return a list of all word tokens (Lettters, digits, underscore)."""
